on: [push]

jobs:
  build: 
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: degub
        run: ls -la
      - uses: actions/setup-go@v4
        with:
          go-version: '1.21'
      - run: go build ./..
      - run: go test ./..